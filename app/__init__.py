"""Claude Subscription Gateway.

An OpenAI-compatible API gateway that proxies requests to Claude through the
Claude Agent SDK, which authenticates using an existing Claude Code
subscription login (no Anthropic API key required).
"""

__version__ = "0.1.0"
