apiVersion: v1
kind: Service
metadata:
  name: {{ include "claude-sub-gateway.fullname" . }}
  labels:
    {{- include "claude-sub-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "claude-sub-gateway.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
